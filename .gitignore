/target
/target/**
/.idea
/Cargo.lock.bak
**/*.rs.bk
.DS_Store
*.swp

# aarch64 cross-compile artifacts
target/aarch64-unknown-linux-gnu/

# bindgen output directories
target/build-*/mpp-bindings
target/build-*/out

# repowise local state — auto-regenerated, no value in source control
/.repowise/
/.claude/CLAUDE.md

# Per-developer Claude Code config (allow-list); untracked since 8af677d-era cleanup
/.claude/settings.local.json

# kimi-code plugin-managed skills — per-machine tooling, not project source
/.kimi-code/

# speckit session state — "current feature" pointer is per-developer;
# templates/scripts under .specify/ stay tracked as shared workflow config
/.specify/feature.json
/.vscode

# Per-developer Claude Code MCP server config (mirrors settings.local.json)
/.mcp.json
/assets/oceans_baseline.mp4
/assets/oceans_processed.mp4
/assets/test_800x480_call.mp4
